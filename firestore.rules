rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permite leitura e escrita para todas as coleções e subcoleções (users, routines, progress, alerts, activities, etc.)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
